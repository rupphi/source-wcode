# jDesk Migration Evidence — 2026-07-18

Đây là evidence checkpoint, không phải tuyên bố migration hoàn tất. Mọi token/secret và absolute
app-data path đều bị loại khỏi tài liệu.

Cập nhật gần nhất: 2026-07-19.

## Reproducible gates

- `./gradlew clean test bindings jdeskFrontendBuild jdeskDoctor`: **PASS** trên macOS arm64,
  Java 25.0.3; 220 tests, 0 failures/errors (7 opt-in live tests skipped in the default gate),
  generated bindings, WebKit, `jlink`, `jpackage` và npm đều pass.
- `./mvnw -B verify`: **PASS**, 206 tests, 0 failures/errors/skips.
- Frontend: lint, strict typecheck, 71 behavior tests, production build và `npm audit` (0
  vulnerabilities) đã pass qua dashboard/sync, supply, packing, Excel-import và template-catalog
  cùng GTIN-mapping/Znack-local increments.
- `git diff --check`: **PASS** trước mỗi commit.

## Native jDesk evidence

- Packaged-content runtime smoke start/stop pass.
- Native automation mở workspace từ live app-data, đọc 8 shop đã cấu hình, render local KPI,
  không có API key field/type/DOM value và không ghi secret ra console.
- Native automation bấm đồng bộ, quan sát loading/progress/success, chờ job hoàn tất khoảng 5 giây,
  rồi thấy KPI refresh. Automation console không có lỗi chưa xử lý.
- Native automation mở supply workspace trên live SQLite: 863 supply/35 trang, trang 2 trả 25
  dòng, filter open trả đúng 1 dòng; active navigation, pagination và status đều khớp. Snapshot
  WebView 1440px đã được review, console rỗng và DOM không chứa API-key field/token.
- Từ live supply list, native automation mở detail có 2 orders, render order ID dưới dạng chuỗi,
  tìm/sort/page contract có giới hạn, tắt sort theo size rồi quay lại list vẫn giữ state. Detail DTO
  và DOM không có remote image URL/token; console vẫn rỗng.
- Hai product thumbnail trong live supply detail được stream từ local PNG cache qua
  `jdesk://app/order-images/<opaque-token>.png`: cả hai load hoàn chỉnh (`246×328`), không có
  `http(s)` image URL hoặc secret trong DOM, asset requests trả 200 và automation console rỗng.
  Route chỉ nhận GET/HEAD, dùng runtime HMAC token, giới hạn từng ảnh 5 MiB và LRU cache 64 MiB.
- Native detail refresh chạy background job qua official read operations `GET /api/v3/orders`,
  `GET /api/marketplace/v3/supplies/{supplyId}/order-ids` và `POST /api/v3/orders/status` (POST này
  chỉ đọc status). Live WB xác nhận 2 order cũ đã được chuyển sang supply khác, nên supply đang mở
  cập nhật chính xác từ 2 xuống 0. Detail selection vẫn giữ nguyên trong lúc chạy; khi quay lại,
  cùng list/filter/page tự reload với count 0. Console rỗng, DOM không có secret và không gọi
  seller-state mutation endpoint.
- Native open dialog đã import một XLSX tạm chứa 2 order hiện tại của shop đã cấu hình. Java chỉ
  trả basename và opaque session qua bridge; absolute path, remote URL, token và secret field đều
  không xuất hiện trong response/DOM/console. Official sticker request trả 2/2 sticker, danh sách
  render đúng natural order và console rỗng. Luồng này không ghi SQLite và không thay đổi
  seller-state.
- XLSX parser chặn macro, file không phải XLSX, ZIP bomb/entry quá lớn, sheet/order/image vượt
  quota và order ID trùng/không hợp lệ trước khi giữ session. Sticker adapter kiểm thử riêng
  request/auth contract, malformed success, 401/429 và không giữ response body nhạy cảm.
- Native print smoke chạy trên bản sao SQLite của live app-data với một shop đã cấu hình và supply
  mở gồm 5 orders. KIZ preflight đọc metadata WB trước native save; cả 5 order đã có KIZ áp dụng
  sẵn, không có local KIZ nào được reserve/consume và response xác nhận 0 background attachment.
  Luồng sau đó lấy 5/5 sticker, mở `NSSavePanel`, tạo nhãn PDF 1.7 gồm 10 trang và companion picking
  sheet 1 trang, ghi print job `success` với 5 items, rồi mở file qua opaque Java session. Đây chỉ
  dùng read/generate endpoints; không gọi seller-state mutation endpoint.
- Print result chỉ đưa UUID session, basename, string job ID và bounded counts qua bridge. Native
  automation xác nhận DOM/console không chứa absolute path, token hoặc remote URL; console rỗng và
  hai file cùng bản sao DB/screenshot tạm đã được chuyển vào Trash sau kiểm tra.
- Live smoke phát hiện cả client và runtime jDesk đều có deadline mặc định 30 giây, không phù hợp
  command chứa native save panel. Hai phía hiện dùng cùng giới hạn 10 phút chỉ cho
  `printing.exportSupply`; toàn bộ transaction chạy trên virtual invocation thread để interrupt
  dừng trước export, và regression tests chứng minh các command khác vẫn giữ timeout mặc định.
- Native packing board đọc shop KingRussia từ live app-data và trả đúng 3 đơn mới, 1 supply đang
  chuẩn bị, 862 supply đã đóng. Category filter `Кардиганы` thu danh sách từ 3 xuống đúng 1 order;
  tab chuẩn bị render supply `WB-GI-250870897`; tab dispatch chỉ render 20 hàng/trang và tính đúng
  44 trang, chuyển sang trang 2 thành công. Hai ảnh local đi qua opaque asset route và trả 200.
  Accessibility tree, bridge log và ảnh riêng cửa sổ ở 1440×900 cùng min-size 960×640 không có
  lỗi/tràn ngang/secret. Luồng chỉ đọc SQLite/cache, không gọi seller-state mutation của WB.
- Print history live smoke đọc các job cục bộ qua bounded page/search/status contract mà không trả
  `template_layout_json`, raw error hoặc token. Native view của KingRussia hiển thị đúng 4 job
  thành công, 0 job lỗi; job ID đi qua bridge dưới dạng string và tên file/path không xuất hiện
  trong list response ngoài các basename allowlisted sau export.
- Native history reprint trên job `34`/supply `WB-GI-250870897` mở `NSSavePanel`, tạo lại 2 nhãn
  thành PDF 4 trang 58×40 mm và companion picking sheet 1 trang A4. UI chỉ nhận UUID session TTL
  30 phút, basename và bounded count; open command từ chối forged/cross-shop/expired session.
  SQLite vẫn giữ nguyên 34 print jobs với max ID 34 và không có WB mutation. Accessibility tree,
  bridge log và ảnh riêng cửa sổ ở 1440×900 cùng 960×640 không có lỗi/tràn ngang/secret. Cửa sổ
  được khôi phục trước khi đóng; PDF/screenshot tạm được chuyển vào Trash.
- Cả client và runtime dùng deadline 10 phút chỉ cho `printing.reprintHistory`, bao trọn thời gian
  người dùng thao tác native save panel; interrupt hủy toàn bộ transaction trước khi export.
- Native template designer đọc riêng catalog FBS/FBO thật từ local SQLite qua DTO allowlist, không
  đưa raw `layout_json` qua bridge. FBS trả template mặc định với 10 layer; FBO trả catalog
  `Default`. Preview đặt KIZ/Code128/text/separator/sticker-tail theo geometry đã đổi từ point sang
  mm, inspector làm tròn đến 0,01 mm và mode radio giữ đúng trạng thái. Accessibility tree cùng ảnh
  riêng cửa sổ ở 1440×900 và 960×640 không có lỗi/tràn ngang/secret. Đây là luồng local-only,
  không gọi WB/Znack và không ghi seller-state.
- Native template writer chạy trên bản sao SQLite nhất quán của live app-data và hoàn tất
  create → rename → sửa geometry/visibility → save → set-default → reset → duplicate → delete.
  Một vòng riêng thêm phần tử từ palette, copy/paste/delete, save rồi reset về đúng 10 layer.
  Sau smoke, FBS/FBO đều trở lại đúng một template, không còn tên test/dirty dialog; automation
  console có 0 dòng lỗi. AX tree ở 1440×900 và 960×640 thấy 43 button cùng các action create/save/
  add; DOM không tràn ngang. Bản sao DB, descriptor và screenshot tạm đã được chuyển vào Trash.
- Native FBO catalog đọc live local data của KingRussia theo đúng page 50 SKU. Automation đi
  page 1 → 2 → 1 nhưng DOM luôn chỉ giữ 50 card; quantity `2` và summary `2 пары · 1 SKU` được giữ
  nguyên. Exact SKU search trả một card, filter `Кардиганы` trả toàn bộ card đúng subject và 8
  subject khả dụng. Trang đầu tải 50/50 ảnh qua opaque `jdesk://app/order-images/...` route; asset
  request đều 200, DOM không chứa `http(s)`/token/path và console rỗng.
- FBO WebView đã được review ở 1440×900 và min-size 960×640 sau bounded-pagination remediation.
  Cả hai không horizontal overflow; ở min-size toàn bộ 214 button/input/select có accessible name.
  User window bounds được backup rồi khôi phục về đúng 1440×857 sau smoke. macOS không cấp AX cho
  `osascript` trong lần chạy này, nên evidence accessibility của slice dùng semantic DOM/runtime
  automation thay vì tuyên bố AX tree.
- `fbo.export` re-resolve SKU theo shop, giới hạn 500 SKU/10.000 pair, reserve KIZ trước render,
  consume ngay trước atomic publish và release khi lỗi trước publish. Regression tests cover
  cancel/interruption, resolver mismatch, invalid/duplicate input, invalid plan, planner/export
  failure, late publish failure, safe error redaction, cross-shop/opaque TTL session và client +
  runtime deadline 10 phút. Integration test dùng SQLite tạm và exporter thật tạo đúng PDF hai
  trang 58×40 mm cho một pair không KIZ; live smoke không bấm export nên không consume KIZ thật.
- Native GTIN workspace đọc live app-data của KingRussia ở cửa sổ 1440×857 và trả empty state đúng
  với catalog local hiện có 0 GTIN. Semantic controls đều có accessible name, DOM không tràn ngang,
  không có remote `src`/`href`, token hay lỗi console; smoke chỉ đọc SQLite và không gọi
  WB/Znack.
- Mapping lifecycle chạy riêng trên SQLite tổng hợp cô lập ở min-size 960×640 với 58 GTIN, 4
  subject và các wildcard/exact rule thuộc nhiều owner. Catalog giữ đúng giới hạn 50 dòng ở trang
  1, 8 dòng ở trang 2; inventory/status, modal ba cột, current/foreign ownership và disabled rule
  đều render đúng, không tràn ngang và console rỗng.
- Native input đổi một wildcard thành exact female rồi save qua command thật. Sau checkpoint,
  transaction chỉ thay rule của GTIN đang sửa, giữ nguyên exact rule khác, hai foreign-owner rule
  và cả 4 KIZ code. Một vòng sửa rồi cancel trước đó không đổi database. User window bounds được
  backup và khôi phục byte-for-byte; database, descriptor và screenshots tổng hợp được chuyển vào
  Trash sau kiểm tra.
- Native Znack workspace chạy trên SQLite tổng hợp cô lập với một settings snapshot và 58 GTIN.
  Ở 1440×857, catalog dùng bảng năm cột; ở min-size 960×640, cùng dữ liệu chuyển thành card mà
  không tràn ngang. Active catalog giữ đúng page 50/5, deleted catalog tách riêng; toàn bộ control
  có accessible name, remote `src`/`href` rỗng và console không có lỗi.
- Settings DTO/native DOM chỉ có OMS, goods document, auto-introduction và certificate summary đã
  sanitize; synthetic API key, endpoint, selector/thumbprint, executable path/argument và metadata
  JSON không xuất hiện trong HTML. Native `/input` thật toggle và save auto-introduction rồi khôi
  phục; private signer/path vẫn nguyên sau checkpoint.
- Native visibility lifecycle chọn GTIN `04601234560041`, hide làm active page 2 từ 5 xuống 4 và
  deleted từ 3 lên 4, sau đó restore về đúng 55 active/3 deleted. SQLite audit có chính xác một
  `GTIN_DELETE/HIDDEN` và một `GTIN_RESTORE/RESTORED` cho target; settings/các GTIN khác giữ nguyên.
  Không gọi remote sync, CryptoPro, purchase/introduction hoặc live-shop mutation. User window
  bounds được khôi phục byte-for-byte; app-data, descriptor và screenshots tổng hợp được chuyển
  vào Trash sau kiểm tra.
- Certificate bridge dùng discovery session/certificate UUID opaque, shop scope và TTL 10 phút;
  selector/thumbprint/provider/raw subject vẫn ở Java. Command tests cover replacement/replay,
  expired/keyless/stale selection, CryptoPro error allowlist, successful signing delegation và
  atomic settings+audit persistence; SQLite conflict test chứng minh stale snapshot rollback cả
  settings lẫn audit.
- Participant product sync chạy một background job mỗi shop, re-resolve opaque settings version
  trong worker, coalesce duplicate start, poll safe phase/count, cooperative cancel và redacted
  failure. Tests cover completion, cancellation, unavailable upstream và race đổi settings trước
  API; legacy product-service integration tests vẫn cover auth/paging/catalog enrichment/local save.
- Native automation mới chạy trên app-data cô lập ở 1440×857: settings panel render CryptoPro
  discovery, `/input` thật trả empty certificate state an toàn trên host này; product panel render
  đúng 2 fixture GTIN và khóa sync vì signature chưa verified. Cả hai có 0 horizontal overflow,
  0 control thiếu accessible name, console rỗng và không có API key/executable path/thumbprint
  trong DOM/attributes. Checkpoint giữ signer rỗng, 0 audit và đúng 2 product; không ký hay gọi
  remote Znack. App-data/descriptor/screenshots được chuyển vào Trash và user window-state được
  khôi phục byte-for-byte.
- Purchase pipeline dùng UUID persist trước remote call và unique theo shop. Domain/command tests
  chứng minh replay cùng request trong lúc chạy, sau hoàn tất và sau restart không tạo pipeline/đơn
  thứ hai; stale settings dừng trước remote. DTO chỉ đưa stage/state allowlist, tổng số mã tải,
  progress và safe error kind; journal chỉ đưa action/severity/message/HTTP class đã allowlist.
- Native purchase workspace chạy trên app-data cô lập ở 1440×857 với ba seeded state: một
  `CREATING_ORDER` mơ hồ bị khóa auto-retry để tránh tính phí hai lần, một
  `INTRODUCTION_FAILED` chỉ cho phép lặp lại document input, và một completed pipeline hiển thị
  đúng 2/2 mã dưới dạng aggregate. Progress lần lượt 15/100/100; dialog mua ban đầu chỉ có bước
  chuẩn bị cục bộ, dialog retry nói rõ mã đã mua sẽ không được đặt lại.
- Journal native hiển thị đúng ba bản ghi đã sanitize; DOM không chứa fake KIZ, external order/
  document id, certificate selector/path hay payload. Purchase/journal có 0 horizontal overflow,
  0 control thiếu accessible name và console rỗng. Checkpoint SQLite giữ nguyên 3 pipeline, 2 đơn,
  5 mã, 3 log và 0 introduction document. Không gọi prepare/start/retry, không ký và không thực hiện
  remote Znack mutation. User window-state được khôi phục byte-for-byte; toàn bộ app-data,
  descriptor và screenshot thử nghiệm được chuyển vào Trash.
- License adapter giữ nguyên `LicenseService`, Ed25519 verifier, 14-day offline grace và clock
  rollback oracle. Status/action DTO không trả key, fingerprint, signed payload, server URL, path
  hay raw error; tests cover toàn bộ tám status, key normalization/redaction, network failure,
  offline refresh, explicit deactivation và serialized concurrent mutation.
- Paid KIZ prepare/start được chặn lại ở Java bằng cùng `LicenseService.kizAllowed()` oracle; React
  cũng khóa purchase action, nhưng persisted purchase replay vẫn đọc được để recovery không bị mất.
- Native license settings chạy trên app-data trống cô lập ở 1440×857: render đúng not-activated,
  activation disabled cho key sai định dạng, 0 horizontal overflow, 0 control thiếu accessible
  name, 0 remote `src`/`href`, console rỗng và DOM không chứa key/fingerprint/payload/path/server
  URL. Process chỉ có automation listener loopback, app-data không tạo `license.json`, nên không
  gọi production license server hay thực hiện activation/deactivation. SQLite integrity pass;
  user window-state được khôi phục byte-for-byte và app-data/descriptor/screenshot được chuyển vào
  Trash.
- Preference adapter dùng lại đúng hai row `app_language`/`app_theme`, allowlist RU/EN/VI/ZH và
  dark/light/system, normalize dữ liệu cũ hỏng và serialize mutation đồng thời. React runtime cũng
  từ chối response tùy ý trước khi thay root attributes hoặc đưa dữ liệu vào DOM.
- Browser runtime kiểm tra dark pre-bootstrap, explicit light và system dưới cả OS light/dark:
  computed `color-scheme`, canvas/text tokens đều đổi đúng, không tràn ngang và console sạch.
- Native preference smoke chạy trên app-data trống cô lập: đổi RU/dark → EN/light, restart xác nhận
  EN/light được nạp lại, đổi tiếp sang system trên macOS dark rồi restart lần hai xác nhận EN/system.
  Settings có 0 control thiếu accessible name, 0 remote `src`/`href`, 0 horizontal overflow và
  console rỗng; SQLite lưu đúng hai giá trị và `integrity_check` trả `ok`. Process chỉ mở listener
  automation loopback. User window-state được khôi phục byte-for-byte với SHA-256 ban đầu; app-data,
  descriptor và screenshot tạm được chuyển vào Trash. Evidence này chỉ xác nhận shared shell,
  settings và license; copy tiếng Nga trong các feature view còn lại vẫn là parity backlog.
- Diagnostics thay legacy auto-upload có license/device/shop/raw-derived message bằng summary local
  read-only và native ZIP export explicit. Java/React tests dùng canary secret/path/shop trong
  SQLite và malformed bridge/error, chứng minh DTO, receipt, ZIP và DOM chỉ có version/platform,
  health enum, bounded aggregate counts cùng boolean export/cancel; database chưa tồn tại trả
  `unavailable` mà không tự tạo app-data.
- Native macOS smoke trên app-data trống cô lập mở Help ở 1440×857: dialog có 0 control thiếu
  accessible name, 0 horizontal overflow, 0 remote URL và console rỗng. Save thật tạo ZIP 575 byte
  quyền owner-only với đúng `diagnostics.json` + `README.txt`; manifest có 0 shop và không có path,
  hostname, token, license/fingerprint, log, SQL hay stack. Một vòng native cancel riêng không ghi
  file, focus trở lại Help, SQLite vẫn `integrity_check=ok` và 0 shop/print job. Không có network
  support upload; app-data, descriptor, screenshot và ZIP tổng hợp đã chuyển vào Trash.
- Signed updater có verifier Ed25519 verify payload bytes trước JSON, exact schema/key/repo/tag/MSI,
  envelope/payload/note/size/hash bounds và fixed latest-manifest endpoint. Java tests cover tamper,
  wrong host/tag/name/size/hash, oversized envelope, safe bridge metadata, explicit state machine,
  duplicate/reuse/cancel, optional shared skip, mandatory no-skip, owner-only part,
  short/long/hash-mismatch cleanup, repeated Authenticode, snapshot ordering,
  mutation-during-snapshot TOCTOU và helper chờ đúng WCode PID trước khi gọi MSI.
- React tests cover no automatic check/download, malicious response redaction, mandatory advisory,
  explicit download, progress/cancel chỉ báo hoàn tất sau backend cleanup, second install confirmation
  và optional skip. Node tests create
  a real Ed25519 pair, independently verify the canonical release envelope and reject mismatched
  keys, unsafe notes/version and undersized MSI. Workflow YAML parses locally and requires scoped
  release secrets, stable Windows upgrade UUID, signed launcher/EXE/MSI, post-sign MSI hash, draft
  publication and independent manifest match. Đây là automated evidence; chưa có Windows x64
  install/failure/rollback rehearsal nên updater chưa được nâng lên Parity.
- Native macOS safe-state chạy trên app-data trống cô lập ở 1440×857, không có release key/publisher:
  mở Settings không tạo network hay update artifact; trước/sau explicit check process chỉ có một
  automation listener loopback. Check fail closed bằng localized safe message, không render URL,
  SHA/signature hoặc nút download/install; dialog có 0 unnamed control, 0 remote URL, 0 horizontal
  overflow và console rỗng. App-data không có `update-staging`/snapshot, SQLite `integrity_check=ok`
  với 0 shop; đóng dialog trả focus về Settings. Descriptor, app-data và screenshot 2880×1714 đã
  được chuyển vào Trash sau khi xác nhận process dừng.
- Shop command dùng DTO bounded chỉ có `id/name/tokenConfigured`; create/update/select/delete được
  serialize, generated ID và selection commit cùng transaction, còn `Shop.toString()` chỉ báo cờ
  configured. SQLite fault tests chứng minh create rollback khi selection write fail, token trống
  khi edit giữ token hiện tại, cascade đúng và active Znack pipeline chặn delete. Gate `StampedLock`
  theo shop giữ lease xuyên virtual worker WB/supply/Znack product-sync: test race chứng minh worker
  đang chạy chặn delete và delete độc quyền chặn job mới, không có cửa sổ TOCTOU giữa hai bước.
- Native shop lifecycle chạy trên app-data trống cô lập ở 1440×857 với token tổng hợp: create tự
  chọn shop mới → replace token/name → edit lần nữa với token trống giữ nguyên credential → mở cảnh
  báo cascade → confirmed delete. Checkpoint SQLite lần lượt xác nhận generated ID, selection, token
  replacement/retention, child row cascade, selection rỗng sau delete và `integrity_check=ok`.
  Sau mỗi save dialog/password input đã unmount và cả token cũ/mới không có trong HTML; cuối smoke có
  0 remote URL, 0 control thiếu accessible name, 0 horizontal overflow và console rỗng. Không gọi
  WB/Znack, không mở app-data thật; database/descriptor/screenshot cô lập đã chuyển vào Trash.
  Credential vẫn cố ý authoritative trong `shops.api_key` cho JavaFX rollback; evidence CRUD này
  đứng trước increment OS-store bên dưới và tự nó không tuyên bố secret-at-rest parity.
- Credential dual-write tạo additive metadata sau snapshot gate: version chỉ tăng khi fingerprint
  token thực sự đổi, kể cả phát hiện JavaFX update ngoài luồng; SQLite legacy commit trước rồi
  SecretStore `put/get` read-back mới ack mirrored version/fingerprint. Fault tests cover lỗi trước
  và sau `put`, lỗi `get`, corrupt/missing/stale OS entry, lỗi SQLite ack, cùng serialization giữa
  reconcile và mutation; mọi retry giữ token legacy mới nhất và không tạo shop trùng.
- Delete ghi tombstone chỉ gồm random bounded secret-key/version/fingerprint trong cùng transaction
  trước cascade. Fault tests cover lỗi trước/sau OS delete và lỗi tombstone ack; retry idempotent
  cuối cùng dọn cả entry lẫn tombstone. Marker migration cũ không thể bỏ qua pre-schema snapshot,
  và hai bảng additive vẫn tương thích với JavaFX rollback.
- Native macOS lifecycle chạy trên app-data/token tổng hợp cô lập: create ghi Keychain generic
  password service `jdesk:com.tuandev.wcode`, SQLite/Keychain read-back ack version 1; replace token
  nâng cả credential/mirrored version lên 2; confirmed delete đưa shops/mirror/tombstone về 0 và
  `security find-generic-password` xác nhận account đã biến mất. Cold restart có marker migration,
  `integrity_check=ok`, console rỗng, 0 password input/remote URL/overflow và không còn tên shop.
  Không gọi WB/Znack hay mở app-data thật; app-data/descriptor cô lập đã chuyển vào Trash. Vì
  plaintext legacy còn bắt buộc trong rollback window và chưa có Windows Credential Manager smoke,
  row vẫn là `Foundation (dual-write rollback)`, chưa phải secret-at-rest parity.
- Một stale supply detail trả 404 trong live data; adapter xử lý an toàn và overview job vẫn hoàn
  tất. Đây không phải seller-state mutation.

## Wildberries live evidence

- `wildberries.syncOverview` dùng một shop đã cấu hình và **PASS** read-only live smoke. Secret chỉ
  được đọc trong Java; UI/result/log evidence không chứa token. Local SQLite cache writes là thay
  đổi được phép và đã chạy dưới shared app-data lock.
- Existing full `WbSyncWorkflow.syncAll` diagnostic trước đó đã gặp HTTP 500 lặp lại tại
  `GET /api/v3/orders` trên nhiều shop. Supply-scoped refresh sau đó đã PASS endpoint này cùng
  order-ids/status, nhưng toàn bộ `syncAll` pipeline vẫn chưa được re-verify end-to-end và chưa
  được tính parity. Không có remote seller-state mutation nào được thực hiện.

## Data ownership and recovery evidence

- JavaFX và jDesk dùng cùng lock trước `Database.initDatabase()`; contention/release tests pass.
- Snapshot dùng SQLite online backup, gồm committed WAL state, rồi integrity + SHA-256 verify.
- Offline restore tạo forensic bundle byte-for-byte của `database.db`, WAL và SHM trước mutation,
  kể cả khi live DB corrupt. Restore từ chối filesystem không hỗ trợ atomic replace.
- Durable pending journal được replay trước normal jDesk database bootstrap. Fault-injection tests
  cover crash sau journal, WAL removal, SHM removal, DB replacement và marker publication; cleanup
  failure; snapshot mutation; directory/file symlink; corrupt DB; và trường hợp main DB giống hệt
  restore target nhưng WAL có committed row mới hơn.
- Ready marker có snapshot SHA giờ chỉ được chấp nhận khi còn snapshot cùng SHA verify được; corrupt
  reference tạo rollback point mới thay vì bypass gate. Retention tests giữ toàn bộ snapshot trong
  30 ngày, hai fallback verified mới nhất mỗi reason và một snapshot cho mỗi marker checksum; chỉ
  snapshot verified/hết hạn/đúng layout mới bị xóa, entry lạ/corrupt được giữ làm forensic evidence.
- jDesk 0.1.3 `jdeps` thất bại khi named multi-release dependencies bị đưa làm root classpath. Một
  analysis JAR classes-only không module descriptor tìm đủ 11 JDK modules, trong khi package vẫn
  chứa nguyên `sqlite-jdbc`, SLF4J và mọi runtime JAR. Custom `wcodePackage` pin đúng app version,
  tạo secondary `WCode-Recovery`, checksums và CycloneDX/SPDX; release ký cả hai launcher rồi tái
  sinh evidence trên signed bytes.
- Native macOS app-image `1.2.3` và package mặc định `1.1.7` smoke pass. Packaged recovery launcher
  trên app-data trống chỉ tạo lock và không bootstrap `database.db`; rehearsal riêng seed 1 row →
  packaged jDesk snapshot → thêm row thứ hai → packaged list/verify/confirmed restore, cuối cùng row
  count trở lại 1 và SQLite `integrity_check=ok`. Bốn fixture app-data đã chuyển vào Trash.
- Fresh-context adversarial review sau hai remediation vòng: **APPROVED**, không còn blocker.

Development/canary recovery commands:

```bash
./gradlew wcodeRecovery --args='list'
./gradlew wcodeRecovery --args='verify <snapshot-id>'
./gradlew wcodeRecovery --args='restore <snapshot-id> --confirm'
```

CLI không khởi tạo normal database composition root, yêu cầu exclusive app-data lock, không in
absolute path/exception/secret. Installed app image có secondary launcher native do
`jpackage --add-launcher`; Windows executable/signing/installer vẫn cần release rehearsal thật.

## Open release gates

- External-navigation revocation/system-browser behavior chưa có native adversarial test.
- Windows x64 WebView2, file dialogs, printing 58×40, CryptoPro và signed updater rehearsal chưa có
  evidence máy thật.
- JPMS/native-access production gate chưa pass; preview hiện dùng `ALL-UNNAMED` theo ADR.
- Full `syncAll` từng gặp upstream HTTP 500 và chưa có end-to-end live PASS; supply-scoped refresh
  đã PASS riêng.
- Supply-detail GTIN inventory/buy, packing create/add/deliver + KIZ preflight, live Windows
  CryptoPro/Znack product-sync evidence, approved live Znack purchase/introduction evidence,
  Windows shop credential-store + post-rollback plaintext retirement, approved real license
  activation/deactivation, updater Windows rehearsal và i18n cho các feature view còn lại vẫn còn
  trong parity backlog.
- Legacy PDF KIZ import không nằm trong backlog: release `1.0.28` đã xóa parser/UI/category tables
  có chủ đích để chuyển sang inventory GTIN theo shop; `docs/znack-standalone-module.md` là contract
  hiện hành và cấm tái tạo đường nhập PDF đứng ngoài lifecycle Znack.
